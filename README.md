# Contributing Workflow

$`git clone ...`

... make changes ...

When you wish to update,

```
$git stash
$git pull
$git stash apply # Resolve the conflicts
$git add ...
$git commit -m "..."
$git push
```
